import { getServices } from "./database.js"

const services = getServices()


document.addEventListener("click", event => {

    if (event.target.id.startWith("insert1")) {
        
    }
})